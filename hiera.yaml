---
:hierarchy:
  - "%{location}/%{::hostname}"
  - "%{location}"
  - files
  - common
:backends:
  - yaml
:yaml:
# When specifying a datadir, make sure the directory exists.
  :datadir: "/etc/puppet/puppet/environments/%{::environment}/hieradata"
